window.SIMBA_CONFIG = {
  // Leave empty for local same-origin development.
  // For GitHub Pages, set this to your deployed backend URL, for example:
  // API_BASE_URL: "https://your-backend.onrender.com"
  API_BASE_URL: "",

  // Optional: browser-side Groq key for GitHub Pages demos.
  // Warning: this exposes the key to site visitors, so prefer a backend in production.
  GROQ_API_KEY: "",
};
